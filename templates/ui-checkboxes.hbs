<label for="">{{{label}}}</label>
{{#each items as |item|}}
    <div class="form-check">
  <label class="form-check-label">
    <input type="checkbox" class="form-check-input" name="{{{../id}}}-{{{item.value}}}" id="{{{../id}}}-{{{item.value}}}" value="{{{item.value}}}"{{#if item.checked}} checked{{/if}}>{{{item.label}}}
  </label>
</div>
{{/each}}